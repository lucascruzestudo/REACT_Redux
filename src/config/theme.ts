import { createTheme } from "@mui/material";

export const appTheme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#e0e0e0',
    },
    secondary: {
      main: '#1d50d2',
    },
    text: {
      primary: '#e0e0e0',
    }
  },
});